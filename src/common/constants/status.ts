export enum API_STATUS {
  IDLE = 'idle',
  LOADING = 'loading',
  SUCCEEDED = 'succeeded',
  FAILED = 'failed'
}

export enum ComputerStatus {
  REQUEST = 'request',
  PAID = 'paid',
  INPROGRESS = 'inprogress', 
  REJECTED = 'rejected',
  COMPLETED = 'completed'
}